#Tuples- A built-in data type that lets us create immutable sequences of values. 
# instead of [] brackets, we use ( )paranthesis in tuple
tuple=(2,1,2,9,4,5,3,7,5)
print(tuple[3]) 
# tuple[0]=1 #here, this is invalid since tuples are immutable
print(tuple.index(2))